package oop_abstract_interfaces;

public interface VehicleInterface {

    //Our first interface
    //Interfaces: Are for "What an object does" / "What an entity performs"
    //We want to put in our interfaces - methods that are 'actions' typically, while the main class holds the 'nouns' or 'facts' that define the NATURE of an object (fields and things)

    String logVehicle();
    String turnOn();
}
